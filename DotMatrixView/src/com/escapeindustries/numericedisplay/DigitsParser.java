package com.escapeindustries.numericedisplay;

public class DigitsParser {

	public int[] parse(String input) {
		// Find out if this has already been implemented as some sort of NumberFormat class in the standard library
		// Otherwise use a regular expression to validate? Or just return something bad if it fails validation while
		// parsing?
		
		return new int[0];
	}

}
